**Prerequisites:** `Node.js`, `npm` or `yarn` installed.

Install dependencies:
```
$ npm install
```
or
```
$ yarn install
```
Run the server:
```
$ node index.js
```
Then paste the code from `copyThi
